DATABASE_URL=file:./data/stasis.db
DEEPSEEK_API_KEY=

# AI Provider Keys (only set keys for providers you use)
OPENAI_API_KEY=  # Required for GPT-4o, GPT-4o Mini, o3 Mini models (leave empty if using DeepSeek only)
ANTHROPIC_API_KEY=  # Required for Claude 3.5 Sonnet, Claude 3 Haiku models (leave empty if using DeepSeek only)

AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
